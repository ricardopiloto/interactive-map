# Fixtures de instância legada (spec 099)

As árvores `wfrp/` e `wod/` **podem ser materializadas em tmp** pelos testes
(`tests/legado_helpers.py`: `mapa.db` pré-Alembic + `uploads/`).

Não apontar testes a `/opt/codex-*`.
