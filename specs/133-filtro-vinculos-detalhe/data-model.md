# Data Model: Filtro de tipo de vínculo no painel de detalhe (Relações)

Não aplicável — filtro de UI local, em memória, sobre `Vinculo[]` já carregados; nenhuma entidade, schema ou persistência nova. O único "estado" novo é um `Set<VinculoTipo>` de componente (não persistido), igual em espírito ao `activeTipos` já existente pro grafo geral, mas independente dele.
