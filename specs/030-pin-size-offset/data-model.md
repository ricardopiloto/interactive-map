# Data Model: 030-pin-size-offset

Nenhuma alteração de modelo de dados, schemas ou APIs.

| Entidade (existente) | Uso nesta feature |
|----------------------|-------------------|
| **Local** (`x`, `y`) | Posição do pin; valores persistidos **inalterados** |
| **GrupoPosicao** (`x`, `y`, `formato`) | Posição do marcador do grupo; inalterada |

Esta feature altera apenas a **apresentação** (tamanho e âncora CSS) dos marcadores no mapa.
