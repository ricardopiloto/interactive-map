# Data Model: CTAs da Home respeitam sessão já autenticada

Não aplicável — mudança de navegação condicional sobre um estado de sessão que a aplicação já expõe (`authApi.me()`); nenhuma entidade, schema ou persistência nova.
