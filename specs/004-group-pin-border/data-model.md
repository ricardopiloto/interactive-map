# Data Model: 004-group-pin-border

Nenhuma alteração de modelo de dados, schemas ou APIs.

O ícone do grupo continua derivado de `GrupoPosicao` (`x`, `y`, `formato`) já existente. Esta feature altera apenas a apresentação visual (CSS).
