# Contract: UI components API

**Feature**: 101

Export from `frontend/src/components/ui/`:

- `Button`, `IconButton`, `Input`, `Select`, `Textarea`, `Tabs`, `Chip`, `Card`, `DropdownMenu`, `Tooltip`, `EmptyState`, `Skeleton`, `Dialog`, `Drawer`, `ConfirmDialog`
- `ToastProvider`, `toast`

Props mínimos: ver research; todos usam tokens; touch 40/44 via CSS.
