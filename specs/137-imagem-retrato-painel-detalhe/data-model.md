# Data Model: Exibição da imagem do personagem no painel de detalhe (Relações)

Não aplicável — nenhuma entidade, schema ou persistência nova.

### Dados já existentes (só apresentação)

| Campo | Uso |
|-------|-----|
| `Personagem.retrato_url` | Se truthy, `ImageSlot` renderiza no detalhe; se null/vazio, nada é mostrado |

Sem novos estados de UI além do CSS aplicado à classe `relacoes-page__detail-portrait`.
