# Data Model: 026-smooth-wheel-zoom

**N/A** — sem entidades persistidas, APIs ou schema.

Estado de zoom continua a ser só o transform em memória do `react-zoom-pan-pinch` (`scale`, pan). Esta feature altera apenas a **granularidade** do incremento por evento de roda.
